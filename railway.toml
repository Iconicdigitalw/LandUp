[build]
# Builder defaults to RAILPACK — build and start commands are configured in the Railway dashboard

[deploy]
# Root-level railway.toml — individual services are configured per-service in the Railway dashboard
